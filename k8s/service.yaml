apiVersion: v1
kind: Service
metadata:
  name: k8s-cicd-devops-service
  labels:
    app: k8s-cicd-devops
spec:
  selector:
    app: k8s-cicd-devops
  type: NodePort
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
      nodePort: 30080
