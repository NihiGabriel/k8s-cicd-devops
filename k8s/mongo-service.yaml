apiVersion: v1
kind: Service
metadata:
  name: mongodb-service
  labels:
    app: mongodb
spec:
  selector:
    app: mongodb
  type: ClusterIP             # internal only — not exposed outside the cluster
  ports:
    - protocol: TCP
      port: 27017
      targetPort: 27017
